// If you deploy the Go API somewhere (e.g., Fly.io, Render, Railway, Cloud Run),
// set that base URL here so the site on GitHub Pages can reach it.
// Example: const API_BASE = "https://solara-api.fly.dev";
const API_BASE = "http://localhost:8080";
